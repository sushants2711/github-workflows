name: Learn multiple jobs

on:
  push:
    branches:
        - main




jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Hello world
              run: | 
                echo "This is line one"
                echo "This is line two"